.vs/
*.*~
__pycache__/
.vscode/
# build output: exe/lib and the DLLs + *.dat kernels the post-build step
#	copies in (matches x64/ARM64 at the repo root and nested under each project)
x64/
ARM64/
*.idb
*.pdb
venv/
_codeql_detected_source_root

# Visual Studio per-user project settings (local-only)
*.vcxproj.user

# reproduction / debug logs from the python UI-automation harness
python/*.log

# Amortized course-prior prototype: regenerable sample corpora.
# Built by python/examples/generate_amortized_data.py.
data/amortized_samples/

# UMAP inventory-embedding artifacts: regenerable from the pipeline scripts
# in python/umap/ (see python/umap/README.md). Not checked in — build locally.
python/class_embeddings.json
python/component_embeddings.json
python/umap/inventory_embeddings.npy
python/umap/inventory_meta.json
python/umap/umap_sweep.json
python/umap/umap_viewer/data.json

# Generated clangd compile database (built by tools/emacs/gen-compile-commands.ps1)
compile_commands.json

# Visual Studio "Upgrade C++ Build Tools" wizard artifacts (generated operation records)
.github/upgrades/

# Local Claude Code MCP config: machine-specific absolute paths (Emacs, repo root)
.mcp.json
